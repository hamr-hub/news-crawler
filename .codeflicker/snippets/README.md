# Code Snippets

> 由 bootstrap-ai-flow 自动生成
> 此目录为占位目录，等待 rd-asset-review 从实际项目代码中提取真实 snippets。

## 说明

**此目录将被 rd-asset-review 自动替换为项目实际代码片段。**

运行 `rd-asset-review` 后，AI Flow 会扫描项目源代码，提取高质量的组件、Hooks、工具函数等，替换此目录内容。

## 如何触发提取

在对话中说：**「提取研发资产」** 或 **「初始化 AI Flow」**

## 提取后的目录结构

```
{{PLATFORM_DIR}}/snippets/
├── README.md              # 索引文件（自动生成）
├── components/           # 可复用组件
├── hooks/                # 自定义 Hooks
├── utils/                # 工具函数
├── state/                # 状态管理
├── api/                  # API 服务
└── ...
```

---

*由 bootstrap-ai-flow 自动生成 - 请运行 rd-asset-review 替换为实际代码*
